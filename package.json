{
  "name": "docker-mongodb",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "docker compose -f docker-compose.yml up -d --build",
    "stop": "docker compose -f docker-compose.yaml down -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.2.0"
  }
}
